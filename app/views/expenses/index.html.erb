<p style="color: green"><%= notice %></p>

<% content_for :title, "Expenses" %>

<h1>Expenses</h1>

  <p data-controller="hello">This text will change!</p>

<div class="overflow-x-auto w-full">
  <table class="table table-xs w-full border">
      <%= @expenses.first.decorate.header_row %>
    <tbody>
      <% @expenses.each do |expense| %>
        <%= render partial: "expense_row", locals: {  expense: expense.decorate  } %>
      <% end %>
    </tbody>
  </table>
</div>

<%= link_to "New expense", new_expense_path %>
